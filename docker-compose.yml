services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - tickrprowl_data:/data
    environment:
      - DATABASE_URL=sqlite:////data/tickrprowl.db
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  tickrprowl_data:
